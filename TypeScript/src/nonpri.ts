// array , object
let fishes: string[] = ["salmon", "trout", "tuna"];

let mixedArr: (string | number | boolean)[] = [
  12,
  "eggs",
  "milk",
  true,
  25,
  "apple",
];

mixedArr.push(122);
console.log(fishes);

// tuple
let coordinates: [number, number] = [12, 34];
let couple: [string, string] = ["Husband", "Wife"];
let nameNroll: [string, number] = ["Utsha", 3088];
